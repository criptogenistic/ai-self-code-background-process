make sure to vibecode a separate window as a background app function this utility works best as a second layer in that window
the utility needs access to the code function to rewrite itself (vibecode window)
make sure replies or text input is routed to the function of the lookup code agent
